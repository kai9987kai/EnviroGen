from setuptools import setup, find_packages

setup(
    name="envirogen",
    version="0.1.0",
    description="A Python package for creating 2D and 3D environments with bots, obstacles, and plants.",
    packages=find_packages(),
    install_requires=[
        # Add any dependencies here
    ],
)
